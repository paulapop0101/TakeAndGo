server:
  servlet:
    context-path: /api
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/takeandgo
    username: root
    password: Oanaaaa02.
  jpa:
    hibernate.ddl-auto: validate
    generate-ddl: true
    show-sql: true